# TimSquad Sprint Plan — 2026-03-07

## Summary

- **Total issues**: 13 (all open, field-report based from dogfooding)
- **Total estimated effort**: 27.5 person-days
- **Phase breakdown**: Phase 0 (8.5d) / Phase 1 (10d) / Phase 2 (9d)

---

## Dependency Map

```
#5  → #9   (TeamCreate 안정화 → 핸드오프 개선)
#5  → #11  (상태 동기화 → 컨트롤러 전제조건 검증)
#5  → #16  (TeamCreate → 에이전트 자동 호출)
#17 → #16  (데몬 L2/L3 → 아키텍트 자동 호출)
#10 → #6   (#6 sub-item 1,2는 #10의 중복 → #10 완료 시 해소)
#12 → #15  (E2E 마커 포맷 → 커밋 게이트 검증)
#13 → #14  (DB 스킬 보강 → 스킬 생태계 개선의 구체 사례)
#7  → #14  (스킬 의존성 선언 → 스킬 생태계 성숙도)
```

방향 요약:
```
          ┌─────┐
     ┌───►│ #16 │◄──────┐
     │    └─────┘       │
   ┌─┴──┐           ┌──┴──┐
   │ #5  │           │ #17 │
   └─┬──┘           └─────┘
     │
     ├───► #9
     └───► #11 ←─(coordinate)─ #15 ◄── #12

   #10 ──► #6 (closes sub-items 1,2)
   #13 ──► #14 ◄── #7
   #8  (독립, 내부 의존: semantic → archiv)
```

---

## Phase 0 — Stabilization

**Goal**: E2E 기능 복원 및 핵심 자동화 정상화

| Issue # | Title | Component | Effort | Assignee | Dependencies | DoD |
|---------|-------|-----------|--------|----------|--------------|-----|
| **#5** | Process/Workflow — TeamCreate, E2E 분석, SCR, 상태 동기화 | core-runtime | **L (3d)** | infra | None (root) | 1) TeamCreate 에이전트가 Bash/Edit/Write 도구 실행 (통합 테스트 확인 or 미지원 시 fallback 문서화) 2) `tsq compile` 출력에 `affected_e2e` 필드 포함 — 알려진 UI 파일 컴파일 시 올바른 E2E 매핑 확인 3) 컨트롤러가 다중 태스크 위임 거부 + 순차 단일 태스크로 자동 분할 (단위 테스트) 4) `tsq wf set-phase`가 workflow.json + current-phase.json 원자적 업데이트 (통합 테스트로 desync 없음 확인) |
| **#17** | Daemon — L2/L3 미작동, 관측성, Phase Gate 미강제 | core-runtime | **XL (5d+)** | infra | None (root) | 1) 시퀀스 내 모든 태스크 `completed` 시 데몬이 L2 로그 자동 생성 + `l2_created: true` 설정 2) 페이즈 내 모든 시퀀스 완료 시 L3 로그 자동 생성 3) `tsq wf set-phase`가 게이트 조건(감사 점수, 크리티컬 이슈, 테스트 통과율, 빌드 상태) 평가 후 미충족 시 차단 + 진단 메시지 4) `tsq daemon status` 명령 — pid, uptime, 세션 통계, 자동화 플래그별 실제 발동 횟수 5) 데몬 로그 JSONL 구조화 (이벤트 타입, 타임스탬프, 결과) |

> **Phase 0 소계**: 8.5d (실제 5d+ 가능성 고려 시 ~8d)

---

## Phase 1 — Foundation

**Goal**: 프로세스 무결성 강제, 감사/인덱스 인프라 구축

| Issue # | Title | Component | Effort | Assignee | Dependencies | DoD |
|---------|-------|-----------|--------|----------|--------------|-----|
| **#11** | Controller — 전제조건 자동검증, SCR 강제, E2E 자동화 | core-runtime | **L (3d)** | core | #5 (상태 동기화), coordinate #15 | 1) `tsq delegate` 시 agent.md 전제조건 자동 파싱 → SSOT 파일 미존재/stale 시 차단 + 메시지 2) 다중 파일 복합 태스크 플래그 + 분할 제안 3) `tsq compile --task` 시 `__tests__/e2e/` 자동 grep → `affected_e2e` + `e2e_action` 주입 4) 세 기능 모두 단위 테스트 통과 |
| **#10** | Product-audit — FP Registry, 점수 투명성, Phase D, 재감사 diff | audit/meta | **L (3d)** | core | None | 1) `tsq audit fp add/remove/list` 명령 동작 → 재감사 시 FP 항목 자동 스킵 + 리포트에 "N items excluded (FP)" 표시 2) 감사 리포트 템플릿에 `source` 필드 필수화 → `estimated` 표시 경고 마커 3) SKILL.md Phase D 섹션 (심각도 정렬 remediation, 항목별 검증 타입, 롤백 가이드) 4) 재감사 시 도메인별 점수 delta + 항목별 상태 변경 diff 테이블 출력 |
| **#8** | Meta-index Semantic Layer + Archiv 시스템 | audit/meta | **XL (5d+)** | meta | None (내부: semantic → archiv) | 1) `tsq mi stage <file> --semantic` 시 semantic 필드 (intent, domain, layer, reusability, constraints, method-level) 생성 → JSON 스키마 검증 2) `tsq mi stats`에 semantic coverage % 표시 3) `tsq archiv pack` → .archiv 파일 (manifest.json, meta-index/, source/, context.yaml) 생성 4) `tsq archiv unpack` → 라운드트립 복원 검증 ⚠️ Archiv은 다음 스프린트로 이월 가능 |

> **Phase 1 소계**: 10d (Archiv 이월 시 ~7d)
> **Note**: #8 Archiv 서브시스템은 Semantic Layer 완성 후 별도 스프린트 검토 권장

---

## Phase 2 — Quality & DX

**Goal**: 개발자 경험 개선, 테스트 신뢰성, 스킬 생태계 강화

| Issue # | Title | Component | Effort | Assignee | Dependencies | DoD |
|---------|-------|-----------|--------|----------|--------------|-----|
| **#12** | Testing — E2E Flakiness, 마커 신뢰성, Retry 전략 | testing | **S (1d)** | dx | None | 1) testing 스킬에 E2E 안정성 가이드 (viewport, 셀렉터, serial, retry, wait) 추가 2) `.e2e-passed` 마커 JSON 포맷 (timestamp, exit_code, passed, failed, flaky, source_hash, expires_at) 3) exit_code != 0 시 마커 미생성 4) 타임아웃 가이드: unit 120s, e2e 300s, build 180s |
| **#15** | Guard/Hook — E2E 게이트, Scope Guard, Build Gate | agent-coordination | **M (2d)** | dx | #12 (마커 포맷) | 1) e2e-commit-gate.sh가 JSON 마커 (source_hash, expiry, exit_code) 검증 2) scope guard PreToolUse 훅으로 누적 파일 변경 추적 (3파일 경고, 6파일 차단) 3) 라인 변경 카운트 추가 (>100줄 경고) 4) 모든 guard 스크립트 ShellCheck 통과 |
| **#16** | Agent 조율 — 자동 호출, QA 메인 플로우, Designer 역할 | agent-coordination | **L (3d)** | core | #17, #5 | 1) 시퀀스 완료 시 데몬이 `@tsq-architect` 자동 호출 → 리포트 + L2 로그 생성 2) P3 워크플로우에 QA 스텝 추가 (developer 완료 + vitest 통과 후) → L1/L2 피드백 3) Designer 활성화 기준 문서화 4) 핸드오프 페이로드 구조화 (changed files, test status, open issues) + 스키마 검증 |
| **#13** | Database — Supabase 패턴, Prisma 통합, 커넥션 풀링 | database | **S (1d)** | dx | None | 1) `rules/supabase-patterns.md` (JSONB .contains() 주의, RLS + service key, Edge Runtime init, USE_MOCK 패턴) 2) Database/Prisma 스킬 소유권 경계 문서화 3) Edge Runtime 커넥션 풀링 가이드 4) `tsq knowledge validate` 통과 |
| **#6** | Automation/Daemon — Audit FP, 추정/실측 혼동, Scope Guard 타이밍 | dx | **S (1d)** | dx | #10 (sub-items 1,2 중복) | 1-2) #10 FP Registry + `source` 필드 완료 시 해소 3) scope guard PreToolUse 훅으로 누적 3파일 경고 / 6파일 차단 — Edit 시뮬레이션 테스트로 임계값 트리거 확인 |
| **#7** | Skills/Knowledge — 스킬 의존성 선언, 규칙 중복 해소 | skill-ecosystem | **M (2d)** | core | None | 1) SKILL.md `depends_on` / `conflicts_with` 필드 지원 2) `tsq compile`이 의존성 그래프 검증 (누락/순환 에러) — 테스트 스킬로 검증 3) 최소 1건 실제 중복 제거 (e.g., `_shared/naming-conventions.md`) |
| **#14** | 스킬 생태계 — 스킬 보강, 학습 전이, 성숙도 | skill-ecosystem | **M (2d)** | core | #13, #7 | 1) 핵심 스킬(coding, security) >= 5 rule files; 방법론 스킬(tdd, retrospective) >= 3 files + 예시 2) `tsq retro apply <pattern> --skill <name>` 명령 구현 3) 미반영 패턴 6건 (SP-007, SP-011, SUPABASE-001, DEPLOY-001, E2E-001, FP-007) 적용 완료 |
| **#9** | DX — 에이전트 컨텍스트 전달, 장시간 실행 타임아웃 | dx | **M (2d)** | dx | #5 (benefits) | 1) 에이전트 태스크 완료 시 핸드오프 struct 자동 생성 (changed_files, test_results, warnings, execution_log_ref) — 파일 수정 후 핸드오프 YAML 검증 2) `config.yaml`에 카테고리별 `command_timeout` 설정 (default/test/build/e2e) 3) 데몬이 초과 프로세스 kill + 리포트 (마지막 50줄, 경과시간, 제안) — sleep 999 + 5s 타임아웃 테스트 |

> **Phase 2 소계**: 9d (실행 순서에 따라 #12→#15 순차, 나머지 병렬 가능)

---

## Execution Waves (Phase 2 내부 병렬화)

```
Wave 2-A (병렬): #12(S), #13(S), #6(S)         → 1d
Wave 2-B (병렬): #15(M), #7(M), #9(M)          → 2d  (#15는 #12 완료 후)
Wave 2-C (병렬): #16(L), #14(M)                 → 3d  (#16는 Phase 0 완료 후, #14는 #13+#7 후)
```

최적 병렬 실행 시 Phase 2는 **~6d**로 단축 가능

---

## Effort Summary

| Phase | Issues | Sequential | Parallel (optimal) | Status |
|-------|--------|------------|---------------------|--------|
| Phase 0 | #5(L), #17(XL) | 8.5d | **5.5d** (병렬) | **완료** (2026-03-08, ec1d52d) |
| Phase 1 | #11(L), #10(L), #8(XL) | 10d | **5.5d** (병렬, Archiv 이월) | 대기 |
| Phase 2 | #12(S), #15(M), #16(L), #13(S), #6(S), #7(M), #14(M), #9(M) | 9d | **~6d** (3-wave) | 대기 |
| **Total** | **13 issues** | **27.5d** | **~17d** (최적) | |

---

## Risks & Notes

### Blocking Unknowns
- **#5 sub-item 1 (TeamCreate)**: Claude Agent SDK의 도구 접근 제한이 근본 원인일 수 있음 → 상위 SDK 조사 필요. 미해결 시 fallback 문서화로 DoD 충족
- **#17 (Daemon XL)**: 5d+ 추정이지만 시퀀스/페이즈 완료 감지 로직의 복잡도에 따라 초과 가능. 데몬 아키텍처 리팩토링이 필요할 수 있음
- **#8 Archiv**: 다중 스프린트 규모. Semantic Layer만 이번 스프린트에 포함하고, Archiv 시스템은 다음 스프린트로 이월 권장

### External Input Required
- **#8**: Archiv `.archiv` 포맷 스펙은 PRD에 명시되어 있으나, confidence-based inclusion 메커니즘의 구체 임계값은 PM 확인 필요
- **#13**: Database/Prisma 스킬 소유권 경계 결정 (Option A vs B) 필요 — 이슈 본문에 두 가지 방안 제시됨

### Overlap & Dedup
- **#6 sub-items 1,2**는 **#10**과 중복됨 → #10에서 구현, #6는 sub-item 3(scope guard)만 고유 작업
- **#11 sub-item 3** (E2E 자동 grep)은 **#5 sub-item 2** (E2E 영향 분석)과 연관 → 구현 시 통합 검토
- **#15** (scope guard)와 **#6 sub-item 3** (scope guard PreToolUse)은 동일 작업 → #15에서 구현

### DoD Determination Gaps
- **#16 sub-item 3** (Designer 역할): 이슈 본문에 구체 기준 부족, "문서화"로 DoD 설정 **(inferred)**
- **#14 sub-item 2** (`tsq retro apply`): CLI 인터페이스 상세가 미정, 구현 시 설계 결정 필요

---

*Generated: 2026-03-07 | Source: GitHub Issues #5–#17 (sonature-lab/timsquad)*
